#include <stdio.h>

/**
 * main - Entry point
 * @argc: number of argemts entered on the cl
 * @argv: array of strings of argc
 * Return: 0
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
