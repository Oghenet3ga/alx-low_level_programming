#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: the character
 * Return: 1 if c is uppercase, else 0
 */
int _isupper(int c)
{
	c = 'A';

		if (c >= 'A', c <= 'Z', c++)
			return (1);
		else
			return (0);
}
