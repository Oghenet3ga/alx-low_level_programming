#include "main.h"
#include <stdio.h>

/**
 * _abs - Entry point
 *@n: the character
 * Return: Always 0.
 */
int _abs(int n)
{
	if
		(n < 0)
		_putchar(-1 * n);
	else if
		(n > 0)
		_putchar(n - 0);

	return (0);
}
